# Akash Investigator - backend runtime dependencies.
# Deliberately minimal so the Docker image stays well under the size limit.
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.9.2
# Optional MySQL durability mirror (pure-Python driver, ~0.5 MB). The service
# runs without a DB; this is only used when DB_BACKEND=mysql.
pymysql==1.1.1
